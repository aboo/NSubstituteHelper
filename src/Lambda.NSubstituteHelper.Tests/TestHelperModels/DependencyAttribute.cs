using System;

namespace Lambda.NSubstituteHelper.Tests.TestHelperModels
{
	/// <summary>
	/// Defines an attribute with same name as the one in unity in order to avoid runtime error.
	/// </summary>
	public class DependencyAttribute : Attribute
	{
	}
}
